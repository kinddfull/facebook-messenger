export const AppConfigKey = ['appSecret', 'accessToken', 'verifyToken']

export const PageConfigKey = ['pageId', 'pageToken']

export const Endpoint = 'https://graph.facebook.com'

export const EndpointVersion = 'v2.8'

export const WebhookUrl = 'webhook'
